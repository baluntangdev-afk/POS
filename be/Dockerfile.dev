# Development image: build and run the app
FROM node:20-alpine

WORKDIR /app

# Install dependencies (copied first for better layer caching)
COPY package.json package-lock.json ./
RUN npm i --legacy-peer-deps

# Copy app source, build, then start
COPY . .

RUN npm run build

EXPOSE 3000

CMD ["npm", "run", "start"]
