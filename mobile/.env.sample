ORDERS_LIVE_FEED_WS_URL=https://your-webhook-receiver.example.com
ORDERS_EVENTS_API_BASE_URL=https://your-webhook-receiver.example.com
CLIENT_ID=
WEBHOOK_SECRET=
CSV_EXPORT_PASSWORD=
# Sender for the automatic transactions-CSV email.
# SENDER_APP_PASSWORD must be a Gmail App Password (2-Step Verification on the
# sender account, then Google Account > Security > App passwords), not the
# normal account password.
SENDER_EMAIL=
SENDER_APP_PASSWORD=
